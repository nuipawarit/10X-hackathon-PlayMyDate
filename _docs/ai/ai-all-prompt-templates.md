# 🤖 AI All Prompt Templates

Standard Prompt Templates for Team Usage

---

## 🎯 Purpose

เอกสารนี้จัดทำขึ้นเพื่อ:

- เป็นมาตรฐานการสื่อสารกับ AI สำหรับทั้งทีม
- ใช้แทนแนวคิด “AI Modes” ในเครื่องมือที่ไม่มี UI Mode (เช่น Cursor)
- ลดปัญหา AI ทำงานผิดขอบเขต / hallucination
- ทำให้ผลลัพธ์จาก AI มีความสม่ำเสมอ ตรวจสอบได้ และปลอดภัย

> แนวคิดหลัก
> **Prompt ที่ดี = Role (บทบาท) + Scope (ขอบเขต) + Constraints (ข้อจำกัด)**

---

## 🧠 Global Usage Principles

ก่อนใช้ Template ใด ๆ ต้องยึดหลักดังนี้:

- AI ต้องปฏิบัติตาม **GLOBAL AI RULES** เสมอ
- หากเป็นงานในโปรเจค:
  - ต้องให้ **project-level ai-rules** (ถ้ามี)
- หนึ่ง Prompt ควรมี “บทบาทหลักเดียว”
- ถ้าข้อมูลไม่ครบ → AI ต้องถามกลับก่อน

---

## 🎛️ ORCHESTRATOR MODE

**(วางแผน / แตกงาน / ตัดสินใจกระบวนการ)**

### Prompt Template

```
ทำหน้าที่เป็น Orchestrator

บริบท:
- โปรเจค: <ชื่อโปรเจค>
- เป้าหมาย: <อธิบายสิ่งที่ต้องการ>

หน้าที่:
- วางแผนขั้นตอนการทำงาน
- แยกงานเป็นลำดับที่เหมาะสม
- แนะนำว่าแต่ละขั้นควรใช้ mode ใด

ข้อจำกัด:
- ยังไม่ต้องเขียนโค้ด
- อย่าตัดสินใจแทน business rules
- ถ้าข้อมูลไม่พอ ให้ถามก่อน
```

---

## 🏗️ ARCHITECT MODE

**(ออกแบบระบบ / วิเคราะห์โครงสร้าง)**

### Prompt Template

```
ทำหน้าที่เป็น Architect

บริบท:
- โปรเจค: <ชื่อโปรเจค>
- ปัญหาที่ต้องแก้: <อธิบายปัญหา>

หน้าที่:
- ออกแบบ architecture ระดับภาพรวม
- อธิบาย boundaries และ responsibilities
- วิเคราะห์ trade-offs ของแต่ละทางเลือก

ข้อจำกัด:
- ยังไม่ต้องเขียน production code
- ไม่ผูกกับ framework โดยไม่จำเป็น
- อธิบายเหตุผลของการตัดสินใจทุกครั้ง
```

---

## 💻 CODE MODE

**(เขียน / แก้ไข / ปรับปรุงโค้ด)**

### Prompt Template

```
ทำหน้าที่เป็น Code mode

บริบท:
- โปรเจค: <ชื่อโปรเจค>
- ส่วนที่ต้องแก้หรือเพิ่ม: <อธิบาย>

กฎที่ต้องปฏิบัติตาม:
- ใช้ GLOBAL AI RULES
- ใช้ ai-rules ของโปรเจคนี้ (ถ้ามี)
- รักษาพฤติกรรมเดิม เว้นแต่สั่งให้เปลี่ยน

ข้อกำหนด:
- แก้เฉพาะส่วนที่เกี่ยวข้อง
- หลีกเลี่ยง refactor เกินจำเป็น
- อธิบายเฉพาะจุดที่ไม่ชัดเจน
```

---

## 🐞 DEBUG MODE

**(วิเคราะห์ปัญหา / หา root cause)**

### Prompt Template

```
ทำหน้าที่เป็น Debug mode

บริบท:
- อาการที่พบ: <error หรือ behavior>
- สิ่งที่คาดหวัง: <expected behavior>

หน้าที่:
- วิเคราะห์ root cause อย่างเป็นระบบ
- อธิบายสมมติฐานที่เป็นไปได้
- เสนอแนวทางแก้ไขที่ปลอดภัย

ข้อจำกัด:
- วิเคราะห์ก่อนเสนอวิธีแก้
- หลีกเลี่ยงการเดา
- ยังไม่ต้องแก้โค้ดทันที
```

---

## ❓ ASK MODE

**(ถาม / อธิบาย / ทำความเข้าใจ)**

### Prompt Template

```
ทำหน้าที่เป็น Ask mode

คำถาม:
<คำถามที่ต้องการความเข้าใจ>

ขอบเขต:
- อธิบายให้เข้าใจง่าย
- ไม่ต้องแก้โค้ด
- ไม่ต้องออกแบบระบบใหม่
```

---

## 🔍 REVIEW MODE

**(ตรวจสอบ / วิจารณ์ / ประเมินคุณภาพ)**

### Prompt Template

```
ทำหน้าที่เป็น Review mode

บริบท:
- โค้ดหรือแนวทางที่ให้มา: <แนบหรืออธิบาย>

หน้าที่:
- ตรวจ correctness
- ตรวจ maintainability
- ชี้จุดเสี่ยงหรือ anti-pattern

ข้อจำกัด:
- ไม่ต้องเขียนโค้ดใหม่ทั้งหมด
- ไม่เสนอ solution เกินขอบเขต
```

---

## 🏁 Summary

- Cursor ไม่มี UI Mode → ใช้ **Prompt Template แทน Mode**
- Template เหล่านี้ช่วยให้:
  - AI เข้าใจบทบาทชัด
  - ผลลัพธ์สม่ำเสมอ
  - ลด hallucination และ over-engineering
